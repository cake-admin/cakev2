import { Meta } from '@storybook/addon-docs/blocks';
import tokens from '../tokens/tokens.json';

<Meta title="Foundations/Spacing" />

# Spacing, Radius & Stroke

The cake& spacing tokens, generated from the Figma **Value** variable collection
(`& spacing/Value.tokens.json` → `npm run build:cakeand-tokens` →
`npm run build:cakeand-vars`). Each token is a CSS custom property whose name
mirrors the Figma variable 1:1 — `Spacing/Small/space-100` ⇄
`var(--space-100)`. Use these for every padding, gap, margin, corner, and
border width — never arbitrary pixel values.

export const spacing = tokens.value.spacing;
export const radius = tokens.value.radius;
export const stroke = tokens.value.stroke;
export const MUTED = { fontSize: 12, color: 'var(--color-text-icon-secondary)' };
export const LINE = '1px solid var(--color-stroke-border)';
export const SWATCH = 'var(--color-primary-primary)';
export const SWATCH_SOFT = 'var(--color-primary-primary-overlay)';

## Spacing — `--space-*`

Grouped in Figma as Small (2–10), Medium (12–24), and Large (32–80).

<table style={{ borderCollapse: 'collapse', width: '100%' }}>
  <thead>
    <tr style={{ textAlign: 'left', ...MUTED }}>
      <th style={{ padding: 8 }}>CSS variable</th>
      <th style={{ padding: 8 }}>Figma</th>
      <th style={{ padding: 8 }}>px</th>
      <th style={{ padding: 8, width: '48%' }}>Scale</th>
    </tr>
  </thead>
  <tbody>
    {spacing.map((t) => (
      <tr key={t.name} style={{ borderTop: LINE }}>
        <td style={{ padding: 8 }}><code>--{t.name}</code></td>
        <td style={{ padding: 8, ...MUTED }}>{t.figmaPath}</td>
        <td style={{ padding: 8 }}>{t.px}</td>
        <td style={{ padding: 8 }}>
          <span style={{ display: 'inline-block', height: 14, width: t.px, background: SWATCH, borderRadius: 3 }} />
        </td>
      </tr>
    ))}
  </tbody>
</table>

## Radius — `--radius-*`

`--radius-1000` (999px) is the pill/circle radius used by Button, IconButton,
and Switch.

<div style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginTop: 8 }}>
  {radius.map((t) => (
    <div key={t.name} style={{ textAlign: 'center' }}>
      <div
        style={{
          width: 72,
          height: 72,
          background: SWATCH_SOFT,
          border: `2px solid ${SWATCH}`,
          borderRadius: t.px,
        }}
      />
      <div style={{ fontSize: 12, marginTop: 6 }}>
        <code>--{t.name}</code>
        <div style={{ color: 'var(--color-text-icon-secondary)' }}>{t.px}px</div>
      </div>
    </div>
  ))}
</div>

## Stroke widths — `--stroke-*`

Border and outline widths (e.g. outline Buttons use `--stroke-200`, IconButton
outlines `--stroke-100`, focus rings `--stroke-300`).

<table style={{ borderCollapse: 'collapse', width: '100%', marginTop: 8 }}>
  <thead>
    <tr style={{ textAlign: 'left', ...MUTED }}>
      <th style={{ padding: 8 }}>CSS variable</th>
      <th style={{ padding: 8 }}>px</th>
      <th style={{ padding: 8, width: '55%' }}>Sample</th>
    </tr>
  </thead>
  <tbody>
    {stroke.map((t) => (
      <tr key={t.name} style={{ borderTop: LINE }}>
        <td style={{ padding: 8 }}><code>--{t.name}</code></td>
        <td style={{ padding: 8 }}>{t.px}</td>
        <td style={{ padding: 8 }}>
          <div style={{ width: 180, borderTop: `${t.px}px solid ${SWATCH}` }} />
        </td>
      </tr>
    ))}
  </tbody>
</table>
